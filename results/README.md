# Results

- `frozen/` contains the released result artifacts used for paper-result reproduction.
- `reproduced/` is the destination for outputs generated by the scripts.

Table and figure scripts read the released result artifacts rather than embedding paper values in source code. See `../docs/RESULT_MANIFEST.md` for the source-to-output mapping.
